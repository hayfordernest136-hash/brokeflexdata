{
  "name": "brokeflexdata",
  "version": "1.0.0",
  "description": "Brokeflex Data - Mobile data purchasing platform",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "start": "node backend/server.js",
    "prestart": "npm install --prefix frontend && npm run build --prefix frontend",
    "dev": "npm run dev --prefix backend",
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "lint": "echo \"No linter configured\""
  },
  "dependencies": {
    "axios": "^1.19.0",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-rate-limit": "^8.6.2",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.3",
    "morgan": "^1.10.0",
    "mysql2": "^3.11.3",
    "resend": "^4.0.0",
    "uuid": "^10.0.0"
  }
}
